apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: trilium-data-pvc
  namespace: utilities
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-hostpath
